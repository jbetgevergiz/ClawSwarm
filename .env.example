# ClawSwarm environment template. Copy to .env and fill in values.
# cp .env.example .env

# -----------------------------------------------------------------------------
# Gateway (gRPC server)
# -----------------------------------------------------------------------------
GATEWAY_HOST=[::]
GATEWAY_PORT=50051
# GATEWAY_TLS=0
# GATEWAY_TLS_CERT_FILE=
# GATEWAY_TLS_KEY_FILE=

# -----------------------------------------------------------------------------
# Agent
# -----------------------------------------------------------------------------
# Agent LLM model (default: gpt-4o-mini if unset)
AGENT_MODEL=gpt-4o-mini
# OPENAI_API_KEY=          # if using OpenAI models
# ANTHROPIC_API_KEY=       # for call_claude tool
# EXA_API_KEY="" # For exa_search tool

# -----------------------------------------------------------------------------
# Swarms World (token launch / claim fees tools)
# -----------------------------------------------------------------------------
# SWARMS_API_KEY=    # required for launch_token
# WALLET_PRIVATE_KEY= # wallet private key (base58, or JSON array / base64); used for launch_token and claim_fees

# -----------------------------------------------------------------------------
# Telegram (omit to disable)
# -----------------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=

# -----------------------------------------------------------------------------
# Discord (omit to disable)
# -----------------------------------------------------------------------------
# DISCORD_BOT_TOKEN=
# DISCORD_CHANNEL_IDS=     # comma-separated channel IDs

# -----------------------------------------------------------------------------
# WhatsApp Cloud API (omit to disable)
# -----------------------------------------------------------------------------
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_QUEUE_PATH=    # optional queue path
